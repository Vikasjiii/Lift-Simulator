const floorInput = document.querySelector(".floor-input-container input");
const LiftInput = document.querySelector(".lift-input-container input");
const form = document.querySelector("form");

form.addEventListener("submit", (e) => {
  const floors = floorInput.value;
  const lifts = LiftInput.value;
});
